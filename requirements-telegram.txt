# Telegram-бот с тем же функционалом, что VK (расписание, заметки, напоминания,
# дедлайны, подписки, вход в панель). Без GPT и SOCKS — только aiogram.
#
#   pip install -r requirements-telegram.txt
#   python tg_bot.py

-r requirements.txt

aiogram>=3.20,<4
