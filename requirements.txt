# Runtime-зависимости VK-бота и веб-панели — того, что запускается в проде.
# Раньше здесь лежал pip freeze с чужой машины (pynput, mido, google, dotenv и
# прочее к проекту отношения не имели), и при этом отсутствовал flask-wtf,
# без которого панель не поднимается вообще.
#
#   pip install -r requirements.txt
#
# Тесты и Playwright — в requirements-dev.txt.

# ── VK-бот ───────────────────────────────────────────────────────────────────
vkbottle>=4.10,<5
python-dotenv>=1.0

# ── Разбор Excel с расписанием ───────────────────────────────────────────────
openpyxl>=3.1

# ── Синхронные вызовы VK API (рассылка из панели, резолв имён) ────────────────
requests>=2.31

# ── Веб-панель ───────────────────────────────────────────────────────────────
Flask>=3.0,<4
Flask-WTF>=1.2          # CSRF-защита; без неё web_panel.py падает на импорте
waitress>=3.0           # WSGI-сервер для `python web_panel.py` (локально, CI, смоук)
# Им панель запущена в проде (deploy/vkpanel.service). Маркер платформы —
# не косметика: под Windows gunicorn ставится, но падает на импорте fcntl,
# так что на машине разработчика он только мешает.
gunicorn>=21.2; sys_platform != "win32"
Werkzeug>=3.0           # ProxyFix импортируется напрямую (PANEL_TRUSTED_PROXIES)
